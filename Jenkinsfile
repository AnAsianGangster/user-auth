pipeline {
    agent any

    stages {
        stage('build') {
            // environment {
            //     dotenv_content = 'something'
            // }

            steps {
                sh echo "Hi"
            }
        }

        stage('test') {
            steps {
                sh pwd
            }
        }

        stage('deploy') {
            steps {
                sh pwd
            }
        }
    }
}
