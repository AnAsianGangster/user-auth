pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                echo 'Hi in build'
            }
        }

        stage('test') {
            steps {
                echo 'Hi in test'
            }
        }

        stage('deploy') {
            steps {
                echo 'Hi in deploy'
            }
        }
    }
}
