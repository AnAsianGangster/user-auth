pipeline {
    agent any

    stages {
        stage('build') {
            // environment {
            //     dotenv_content = 'something'
            // }

            steps {
                echo 'Hi'
            }
        }

        stage('test') {
            steps {
                pwd
            }
        }

        stage('deploy') {
            steps {
                pwd
            }
        }
    }
}
